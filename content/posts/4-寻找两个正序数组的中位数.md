---
title: "每日算法 #4 寻找两个正序数组的中位数"
date: "2025-10-11"
draft: false
categories: ["算法"]
tags: ["数据结构", "编程"]
description: 题源力扣
type: daily
---

## 寻找两个正序数组的中位数

### 描述

给定两个大小分别为 `m` 和 `n` 的正序（从小到大）数组 `nums1` 和 `nums2`。请你找出并返回这两个正序数组的 **中位数** 。

算法的时间复杂度应该为 `O(log (m+n))` 。

 

**示例 1：**

```
输入：nums1 = [1,3], nums2 = [2]
输出：2.00000
解释：合并数组 = [1,2,3] ，中位数 2
```

**示例 2：**

```
输入：nums1 = [1,2], nums2 = [3,4]
输出：2.50000
解释：合并数组 = [1,2,3,4] ，中位数 (2 + 3) / 2 = 2.5
```

 

 

**提示：**

- `nums1.length == m`
- `nums2.length == n`
- `0 <= m <= 1000`
- `0 <= n <= 1000`
- `1 <= m + n <= 2000`
- `-106 <= nums1[i], nums2[i] <= 106`



### 核心思想

朴素思想：合并两个数组，排序，判断奇偶个数取中间

符合题目要求的做法：二分，

### 代码

朴素做法

```go
func findMedianSortedArrays(nums1 []int, nums2 []int) float64 {
	// 方法一：合并两个数组，取中位数
	nums := append(nums1, nums2...)
	sort.Ints(nums) // log
	l := len(nums)
	mid := l / 2
	if l%2 == 0 {
		return (float64(nums[mid-1]) + float64(nums[mid])) / 2
	} else {
		return float64(nums[mid])
	}
}
```

